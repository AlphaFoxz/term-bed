{
    "name": "term-bed",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "bun run --cwd ./packages/playground dev",
        "build": "bun run sync && bun run ./scripts/build.ts",
        "sync": "bun run ./scripts/sync-version.ts"
    },
    "devDependencies": {
        "@types/bun": "^1.3.3"
    },
    "peerDependencies": {
        "typescript": "^5.9.3"
    },
    "workspaces": [
        "packages/*"
    ]
}
